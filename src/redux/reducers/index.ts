import { reducer as bookmarks } from '../../features/Bookmarks';
import { reducer as posts } from '../../features/Posts';
import { reducer as sections } from '../../features/Sections';

export default {
  bookmarks,
  posts,
  sections,
};
